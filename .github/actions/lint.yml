name: ESLint Check

on:
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Setup
      uses: ./.github/actions/setup.yml

    - name: Run ESLint
      needs: setup
      run: npm run lint
